/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.7/userguide/tutorial_java_projects.html
 */

plugins {
    // Apply the java plugin to add support for Java
    id 'java'

    // Apply the application plugin to add support for building an application
    id 'application'

    id "com.github.johnrengelman.shadow" version "4.0.2"
}

// Define the main class for the application
mainClassName = 'com.nexmo.cli.App'

dependencies {
    implementation 'com.beust:jcommander:1.71'
    implementation 'com.github.Nexmo:nexmo-java:convert-sms-endpoint-to-json-SNAPSHOT'

    // Use JUnit test framework
    testCompile 'junit:junit:4.12'
}

// In this section you declare where to find the dependencies of your project
repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()

    // Uncomment for non-releases
    maven { url "https://jitpack.io" }
}

jar {
    manifest {
        attributes 'Main-Class': "com.nexmo.cli.App"
    }

    configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
}